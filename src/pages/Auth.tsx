import React from "react";
import LoginForm from "../components/LoginForm";

export const initialValue = {
  email: "",
  password: "",
};

const Auth = () => {
  return <LoginForm />;
};

export default Auth;
